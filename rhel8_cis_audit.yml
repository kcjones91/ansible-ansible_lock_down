---
- name: Run Ansible Lockdown Audit
  hosts: rhel8_cis_lab
  become: yes
  ignore_errors: true
  vars:
    rhel8cis_authselect_custom_profile_name: "my_lab_profile"
    rhel8cis_run_audit: true
    audit_only: true
    fetch_audit_files: true
    audit_capture_files_dir: "/mnt/c/Users/kjon5/vms/afcu"
    audit_output_collection_method: "fetch"

  # pre_tasks:
  #   - name: Ensure audit vars directory exists
  #     file:
  #       path: /opt/RHEL8-CIS-Audit/vars
  #       state: directory
  #       mode: "0755"

  #   - name: Seed host-specific audit vars file (container workaround)
  #     copy:
  #       dest: "/opt/RHEL8-CIS-Audit/vars/{{ ansible_hostname }}.yml"
  #       mode: "0644"
  #       content: |
  #         # Minimal vars needed by the audit templates
  #         machine_uuid: "{{ ansible_facts.machine_id | default('container-lab-uuid') }}"
  #         epoch: "{{ lookup('pipe', 'date +%s') }}"
  #     when: rhel8cis_run_audit | default(false)

  roles:
    - RHEL8-CIS

  # post_tasks:

  #   - name: "FETCH_AUDIT_FILES | Fetch files and copy to controller"
  #     when: audit_output_collection_method == "fetch"
  #     ansible.builtin.fetch:
  #       src: "{{ item }}"
  #       dest: "{{ audit_output_destination }}"
  #       flat: true
  #     failed_when: false
  #     register: discovered_audit_fetch_state
  #     loop:
  #       - "{{ pre_audit_outfile }}"
  #       - "{{ post_audit_outfile }}"
  #     loop_control:
  #       label: "{{ item }}"
  #     become: false

